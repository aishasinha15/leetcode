class Solution {
public:
    int search(vector<int>& nums, int target) {
    
        int n=-1;
        vector<int> A;
         for(int i=0;i<nums.size();i++){
             if(nums[i]==target){
                 n=i;
                 break;
             }
         }
         return n;
    
    }
};
